SELECT 
    f.name AS folder_name,
    s.name AS scan_name,
    sr.scan_run_id,
    sr.scan_start,
    sr.scan_end
FROM 
    folder f
JOIN 
    scan s ON f.folder_id = s.folder_id
JOIN 
    scan_run sr ON s.scan_id = sr.scan_id
ORDER BY 
    f.name, s.name, sr.scan_start;
